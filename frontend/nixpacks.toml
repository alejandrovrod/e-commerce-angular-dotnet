# nixpacks.toml - Angular frontend configuration

[variables]
NODE_ENV = "production"

[phases.install]
cmds = [
  "npm install --production=false"
]

[phases.build]
cmds = [
  "node build-env.js && npm run build:prod"
]

[start]
cmd = "npm run start:prod"
